<div class="card border-success mb-3" style="margin: 10px; padding: 10px">
    <div class="card-body">
{{ form_start(form) }}
    {{ form_widget(form) }}
<table>
    <td><button class="btn btn-success">{{ button_label|default('Ajouter Professeur') }}</button></td>
   <td><a href="{{ path('professeur_index') }}"> <button type="button" class="btn btn-primary">Affichier Les professeurs</button></a></td>

</table>
{{ form_end(form) }}
    </div>
    </div>
